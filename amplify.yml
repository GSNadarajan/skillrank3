version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Prebuild step is not needed"
    build:
      commands:
        - echo "Build step is not needed"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
